// medir  cor cada bloco
   /**botao = 1023;
    lcd.setCursor(0,1); 
    lcd.print("medir verde");
    while(botao < 831){
      botao = analogRead(0);
    }
    int v_verde = ident_cor();
    //
    botao = 1023;        
    lcd.setCursor(0,1); 
    lcd.print("medir vermelho");
    while(botao < 831){
      botao = analogRead(0);
    }
    int v_vermelho = ident_cor();
    //
    botao = 1023;
    lcd.setCursor(0,1); 
    lcd.print("medir amarelo");
    while(botao < 831){
      botao = analogRead(0);
    }
    int v_amarelo = ident_cor();
    //
    botao = 1023;
    lcd.setCursor(0,1); 
    lcd.print("medir preto");
    while(botao < 831){
      botao = analogRead(0);
    }
    int v_preto = ident_cor();
    //
    botao = 1023;
    lcd.setCursor(0,1); 
    lcd.print("medir novo");
    while(botao < 831){
      botao = analogRead(0);
    }
    int v_novo = ident_cor();
    **/

